import React from 'react';
import { storiesOf } from '@storybook/react';

import SiteHeader from './site-header';

storiesOf('Components/Site Header', module).add('default', () => (
  <SiteHeader />
));
